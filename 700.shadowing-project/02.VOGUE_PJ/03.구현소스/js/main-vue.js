// main페이지 vue js

// 컴포넌트 불러오기
import { TopComp, BottomComp } from "./components/common.js";
console.log(TopComp);

// 메인 뷰 인스턴스 생성하기
new Vue({
  // 1. 대상선정
  el: "#app",
  // 2. 데이터 설정
  data() {},
  // 3. 메소드 설정
  methods: {},
  // 4. 라이프사이클 메소드
  // 4-1. created()
  created() {},
  // 4-2 mounted()
  mounted() {},
});
