// 뷰 라우터 세팅 js router.js

/*
    [ 뷰 라우터 인스턴스 내보내기 ]
    라우터 세팅 방식
    new VueRouter({routes: []})
    -> 만약 외부에 세팅될 경우
    const routes = []
    new VueRouter({routes})
    => routes라는 속성명은 정해진 이름임
*/
export default new VueRouter(
    {},
);